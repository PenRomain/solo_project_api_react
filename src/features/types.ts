export interface User {
  id: number;
  name: string;
  username: string;
  email: string;
}

export interface Plainswalker {
  name: string;
  status: string;
  description: string;
  image: string;
  id: number;
}

export interface Pokemon {
  id: number;
  gif: string;
}
